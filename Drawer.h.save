#ifndef DRAWER_H
#define DRAWER_H

#include <vector>

#include <SFML/Graphics.hpp>

#include "Constants.h"
#include "DrawableInterface.h"

class Drawer {
    public:
        static void init();
        static void pushDrawable(DrawableInterface*);
        static void setGUI(DrawableInterface*);

        static sf::View& getView();
        static sf::RenderWindow& getWindow();

        static void draw();
    private:
        static sf::RenderWindow window;
        static sf::View view;
        static std::vector<DrawableInterface*> things;
};

#endif
